<%= form_for [:admin,@paint_colour] do |f| %>
  <% if @paint_colour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paint_colour.errors.count, "error") %> prohibited this paint_colour from being saved:</h2>

      <ul>
      <% @paint_colour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <h1 class="centralise">MANAGE PAINT COLOUR</h1><br>
  <fieldset class="push_both_sm">
    <%= f.label :name, class: "col_3 first" %>
    <%= f.text_field :name, class: "col_9 last" %>
  </fieldset>
  <fieldset class="large col_5 fright">
    <div class="actions nofloat margin_auto block bluebutton">   <%= f.submit "Save" %>  </div>		
  </fieldset>
<% end %>
